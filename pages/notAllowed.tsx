import Heading from '@/ui/heading/Heading'

import Meta from '@/utils/meta/Meta'

const NotAllowed = () => {
	return (
		<Meta title="Not allowed!">
			<Heading title="The functionality is not available for mobile devices!" />
		</Meta>
	)
}

export default NotAllowed
